import 'package:flutter/material.dart';
class cod extends StatelessWidget {
  const cod({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
         return Scaffold(
          body:Center(child:
          Text("ORDER\n PLACED!",style:TextStyle(fontSize: 30)),
          )
         );
  }
}